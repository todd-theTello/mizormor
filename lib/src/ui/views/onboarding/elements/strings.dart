import 'package:flutter/foundation.dart' show immutable;

@immutable

/// Class to holder static constant strings used on the Onboarding view
class OnboardingViewStrings {
  /// onboarding view strings constructor
  const OnboardingViewStrings();
}
